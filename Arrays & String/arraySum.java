public static int arraySum(int[] numbers){
    //Just return the sum of the list
    //Don't print herem
    int sum=0;
    for(int i=0;i<numbers.length;i++){
        sum+=numbers[i];
    }
    return sum;
}
